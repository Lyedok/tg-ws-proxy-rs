# v2.2.3-beta.3

Third prerelease of the minimal OpenWrt integration planned for v2.2.3.

- Replaces the unavailable `install -m` utility with portable `cp`, `chmod` and
  atomic `mv` binary staging supported by minimal OpenWrt images.
- Retains the beta.2 IPK filename/checksum fix and distribution model.
